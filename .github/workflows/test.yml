name: Test and Coverage

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  prepare-resources:
    name: Prepare Downloaded Resources
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 21

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-disabled: false

      - name: Download Resources
        run: ./gradlew downloadMermaid downloadTextmateBundles

      - name: Cache Generated Resources
        uses: actions/cache@v4
        with:
          path: |
            src/main/resources/mermaid
            src/main/resources/textmate
          key: generated-resources-${{ github.run_id }}

  test:
    name: Test (${{ matrix.os }}, Java ${{ matrix.java }})
    needs: prepare-resources
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        java: [21, 23]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ matrix.java }}

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-disabled: false

      - name: Restore Generated Resources
        uses: actions/cache/restore@v4
        with:
          path: |
            src/main/resources/mermaid
            src/main/resources/textmate
          key: generated-resources-${{ github.run_id }}

      - name: Run Tests with Coverage
        run: ./gradlew test jacocoTestReport

      - name: Upload JaCoCo XML Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report-${{ matrix.os }}-java-${{ matrix.java }}
          path: build/reports/jacoco/test/jacocoTestReport.xml
          retention-days: 7

      - name: Upload JaCoCo HTML Report
        if: ${{ always() && matrix.os == 'ubuntu-latest' && matrix.java == 21 }}
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-html-report
          path: build/reports/jacoco/test/html
          retention-days: 7

  coverage-comment:
    name: Add PR Coverage Comment
    needs: test
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all JaCoCo reports
        uses: actions/download-artifact@v4
        with:
          path: jacoco-reports
          pattern: jacoco-report-*
          merge-multiple: true

      - name: Post JaCoCo Report Comment
        uses: Madrapps/jacoco-report@v1.7.2
        with:
          paths: jacoco-reports/jacocoTestReport.xml
          token: ${{ secrets.GITHUB_TOKEN }}
          min-coverage-overall: 0
          min-coverage-changed-files: 0
          title: "Code Coverage Report"
          update-comment: true
