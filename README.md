# Jetbrains Nextflow LSP Support

Basic plugin to add support for the official Nextflow Language Server Protocol (LSP) implementation in Jetbrains IDEs 
like IntelliJ IDEA, PyCharm, WebStorm, etc.

To test locally, `./gradlew runIde`